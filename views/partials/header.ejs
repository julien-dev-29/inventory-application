<header>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">

            <h1>
                Snippet & Co
            </h1>
            <ul class="navbar-nav">
                <li class="nav-item "><a class="nav-link <% if (slug === 'snippets') { %>active<% } %>"
                        href="/snippets">Snippets</a>
                </li>
                <li class="nav-item"><a class="nav-link <% if (slug === 'projects') { %>active<% } %>"
                        href="/projects">Projects</a></li>
                <li class="nav-item"><a class="nav-link <% if (slug === 'tags') { %>active<% } %>" href="/tags">Tags</a>
                </li>
            </ul>
            <div class="p-2 text-end">
                <%= locals.admin ? "Connecté en tant que: " + admin.username :"Pas connecté" %>
                    <% if (locals.admin) { %>
                        <a href="/logout">Logout</a>
                        <% } else { %>
                            <a href="/login">login</a>
                            <% } %>
            </div>
        </div>
    </nav>

</header>